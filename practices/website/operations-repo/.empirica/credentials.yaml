---
# Empirica credentials store for operations-repo
# Zone 2 managed — do not commit to public repo
# IMPORTANT: Add to .gitignore if not already present

supabase:
  project_ref: ksinisdzgtnqzsymhfya
  access_token: ${SUPABASE_ACCESS_TOKEN}
  api_url: https://ksinisdzgtnqzsymhfya.supabase.co

# Note: SUPABASE_ACCESS_TOKEN should be set as an environment variable
# Run: supabase login
# Or: export SUPABASE_ACCESS_TOKEN="your-personal-access-token"
